{"docutils.utils.math.latex2mathml.tex2mathml": "\ndef tex2mathml(tex_math, as_block=False):\n    \"\"\"Return string with MathML code corresponding to `tex_math`.\n\n    Set `as_block` to ``True`` for displayed formulas.\n    \"\"\"\n    math_tree = math(xmlns='http://www.w3.org/1998/Math/MathML')\n    node = math_tree\n    if as_block:\n        math_tree.set('display', 'block')\n        rows = toplevel_code(tex_math).split('\\\\\\\\')\n        if len(rows) > 1:\n            node = mtd()\n            math_tree.append(mtable(mtr(node), CLASS='ams-align', displaystyle=True))\n    parse_latex_math(node, tex_math)\n    math_tree.indent_xml()\n    return math_tree.toxml()", "docutils.utils.math.tex2mathml_extern.blahtexml": "\ndef blahtexml(math_code, as_block=False) -> str:\n    \"\"\"Convert LaTeX math code to MathML with blahtexml__.\n\n    __ http://gva.noekeon.org/blahtexml/\n    \"\"\"\n    args = ['blahtexml', '--mathml', '--indented', '--spacing', 'moderate', '--mathml-encoding', 'raw', '--other-encoding', 'raw', '--doctype-xhtml+mathml', '--annotate-TeX']\n    mathml_args = (' display=\"block\"' if as_block else '')\n    _wrapped = wrap_math_code(math_code, as_block)\n    if '{align*}' in _wrapped:\n        math_code = _wrapped.replace('{align*}', '{aligned}')\n    result = subprocess.run(args, input=math_code, capture_output=True, text=True)\n    if '<error>' in result.stdout:\n        result.stderr = result.stdout[result.stdout.find('<message>') + 9:result.stdout.find('</message>')]\n    else:\n        result.stdout = result.stdout[result.stdout.find('<markup>') + 9:result.stdout.find('</markup>')]\n    _check_result(result)\n    return f'<math xmlns=\"http://www.w3.org/1998/Math/MathML\"{mathml_args}>\\n{result.stdout}</math>'", "docutils.utils.math.tex2mathml_extern.latexml": "\ndef latexml(math_code, as_block=False):\n    \"\"\"Convert LaTeX math code to MathML with LaTeXML__.\n\n    Comprehensive macro support but **very** slow.\n\n    __ http://dlmf.nist.gov/LaTeXML/\n    \"\"\"\n    args1 = ['latexml', '-', '--preload=amsmath', '--preload=amssymb', '--inputencoding=utf8', '--']\n    math_code = document_template % wrap_math_code(math_code, as_block)\n    error_tags = ('Error:', 'Warning:', 'Fatal:')\n    result1 = subprocess.run(args1, input=math_code, capture_output=True, text=True)\n    if result1.stderr:\n        result1.stderr = '\\n'.join((line for line in result1.stderr.splitlines() if line.startswith(error_tags)))\n    _check_result(result1)\n    args2 = ['latexmlpost', '-', '--nonumbersections', '--format=html5', '--omitdoctype', '--noscan', '--nocrossref', '--nographicimages', '--nopictureimages', '--nodefaultresources', '--']\n    result2 = subprocess.run(args2, input=result1.stdout, capture_output=True, text=True)\n    start = result2.stdout.find('<table class=\"ltx_equationgroup')\n    if start != -1:\n        stop = result2.stdout.find('</table>', start) + 8\n        result2.stdout = result2.stdout[start:stop].replace('ltx_equationgroup', 'borderless align-center')\n    else:\n        result2.stdout = result2.stdout[result2.stdout.find('<math'):result2.stdout.find('</math>') + 7]\n    if result2.stdout:\n        _msg_source = result2.stdout\n    else:\n        _msg_source = result2.stderr\n    result2.stderr = '\\n'.join((line for line in _msg_source.splitlines() if line.startswith(error_tags)))\n    _check_result(result2)\n    return result2.stdout"}